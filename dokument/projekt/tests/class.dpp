class Car {
    int x;

    int foo(int one) {
        print(this.x); 
        print( one );
    };
    int bar(int zero, int two) {
        print(zero % two);
    };
    int foobar() {};
}


int main() {
    Car volvo = new Car;
    volvo.x = 5;
    print(volvo.x);
    volvo.foo(3);
    volvo.bar(5, 2);
    volvo.foobar();
}